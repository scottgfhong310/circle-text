# DESIGN — circle-text

> 版本 v1.0｜最後更新 2026-08-04

README 管「怎麼用」，本文管「**為什麼長這樣**」。家族共同規範只標引用、不重複。

## 1. 由來：一支會說謊的原型

前身是 `InProgress/public/lib/page/circle-text-3.html`——1120 行單檔，掛在舊系統
`mantraChakra` 維護畫面的側邊，算完把 JSON 貼回 `fd_cfg_json`，下游用
`rings[].characters` 逐圈切真言文字。

它的數學是對的（幾何、最大餘數法、圈數反解的二次式全部驗過），問題出在**算完之後怎麼呈現**。
以它的預設值（總字數 2066、20 圈、字級 12pt、圈距 14pt、內圈 57 字）實測：

| 畫面上寫的 | 實際產出的 |
|---|---|
| 內圈字數 N₁ = **57** | 第 1 圈 = **47** 字 |
| 字寬/字高 = **12.00 pt** | 實際每字弧長 = **14.55 pt**（+21%） |

它的「配平」是把所有圈的字數乘上 `total / rawSum`（本例 0.8157）——**等於偷偷改掉字距**，
而字級欄、尺寸面板、SVG 半徑標尺全部照舊顯示 12pt。使用者拿這份 JSON 去排 2066 個字，
字會鬆掉兩成，畫面從頭到尾沒有一處透露。

**諷刺的是它已經有正解。** `estimateRingCount()` 做的正是「給定總字數反推圈數」，
但只在**圈數欄位為空**時才會跑，而圈數在 `ready` 就被預設值填滿了——
**出廠設定下，正確的模式走不到。**

本 app 的每一個設計決定都可以回推到這一段。

## 2. 配平＝指定一個自由變數，而不是替使用者決定

要讓總字數命中目標，四個參數必須有一個讓步。與其挑一個然後不講，不如**讓使用者挑，並一律回報代價**。

| 模式 | 解出 | 量化 | 能否精確命中 |
|---|---|---|---|
| `rings` | 圈數 n | 整數 | 否 |
| `gap` | 圈距 g | **連續** | **是** |
| `n1` | 內圈字數 N₁ | 整數 | 否 |
| `scale` | ——（等比縮放字數） | — | 是，代價是字距 |
| `none` | ——（理論值四捨五入） | — | 不適用 |

以同一組預設值實測，四個模式互相印證：

```
模式     解出          總字數  內圈  末圈   實際弧長  誤差%    警告
rings    rings=18       2066    55   175     12.47    +3.95   arcOff
gap      gap=9.31       2066    57   150     12.00     0.00   gapTight
n1       n1=34          2066    34   173     12.04    +0.33   —
scale    —              2066    47   160     14.71   +22.59   arcOff
none     —              2533    57   196     12.00    -0.01   —
```

`scale` 那一列逐格重現原型的輸出（內圈 47、弧長 14.55/格），差別只在**它現在會說**。
`gap` 那一列的 `rawSum` 是 `2066.000000`——連續變數的定義性質，`verify.js` 第 ⑬ 條把它寫成不變量。

**保留 `scale` 不是妥協。** 有些場合就是要維持既定幾何、寧可拉字距（例如版面已定案、
只是換一段長度不同的文字）。錯的從來不是這個做法，是**不講**。

### 恆顯示「實際每字弧長」

`arc.mean = Σ(2πRᵢ) / Σcounts`，與設定字級相差超過 1% 就發 `arcOff` 警告。
這條在 lib 裡是無條件的——**五個模式都算、都回**，`verify.js` 第 ⑭ 條擋著。
幾何與字數必須同時說得通；說不通時，畫面有義務讓它現形。

## 3. 控制器不保存計算結果

原型第二嚴重的 bug：只有三個欄位掛了 `input` listener，另外五個（總字數、圈數、圈距、
邊界、配平）改了完全不觸發；而 Copy JSON 只在快取為空時才補算。實測改成 `total=999`
後按下複製，出去的是舊的 `2066`——**貼進 DB 的切字表與使用者以為的設定是兩回事，而且靜默。**

修法**不是**「記得在每個出口補呼叫 `run()`」，那只是把同一個 bug 推遲到下次改版。
本 app 的結構是：

> **DOM 欄位是唯一輸入來源。** 畫面、複製 JSON、複製連結、下載 SVG
> 各自呼叫 `currentResult()`（＝ `readInputs()` → `Lib.compute()`）重算一次。
> 控制器裡沒有任何 `_snapshot` 之類的狀態可以過期。

代價是每個出口多算一次——幾十圈的浮點運算，量級可忽略。
**換到的是「畫面與輸出不一致」這個 bug 類別在結構上不存在。**

## 4. `buildSvg` 為什麼在 lib

DESIGN_GUIDELINES §4.7 的判準是「呈現引擎自己碰不碰 DOM」。原型用
`document.createElementNS` 逐節點組 SVG，那必須留在控制器。

本 app 改成**純字串組裝**，於是它和 `xlsx-viewer` 的 `buildSheetTable`（資料 → HTML 字串）
同一類，進得了 lib。順帶拿到兩個好處：

- **畫面顯示的與下載的是同一份字串**，不可能不一致（同 `thinking-dot` 的
  `buildCss` ↔ 後端樣板逐字一致的手法）。
- SVG 可以在 Node 裡產生（測試、日後批次出圖），不需要瀏覽器。

文字標籤由呼叫端以 `opts.labels` 傳入——**lib 不依賴 `I18n`**，零依賴這條才守得住。
lib 自己做跳脫（`esc()`），因為標籤終究來自外部。

另外兩個順手修掉的：環狀安全區改用 `fill-rule="evenodd"` 真的挖空（原型是拿背景色蓋一個
實心圓假裝，換背景就露餡）；`width`/`height` 帶 `pt` 後綴，下載的 SVG 在向量軟體開啟
即為實際印刷尺寸（原型宣稱「單位：pt」卻以 px 繪製，標尺小了 25%）。

## 5. 砍掉舊系統 uid，換成深連結

原型會打 `http://127.0.0.1:5000/api/Loc/Sdm/mantraChakra/<uid>` 取回設定——那是舊
MySQL + .NET WebApi 系統，只有 owner 本機才有；家族長期方向明訂舊系統
「只當設計經驗與參考，不是搬遷來源」。獨立 public repo 綁在上面，等於 `clone` 下來不能跑。

改成兩條都不需要後端的路：

- **深連結**：所有參數即查詢欄位，且編輯時以 `history.replaceState` 即時改寫網址。
  於是「複製連結」＝存檔，不需要任何儲存端點。
- **貼上 JSON 匯入**：`parseSnapshot()` 同時吃本 app 的 snapshot、裸輸入物件，
  以及**舊版格式**——舊版的布林 `fit` 對應到 `fit:true → mode 'scale'`／`fit:false → 'none'`，
  所以既有的 `fd_cfg_json` 貼進來就能還原（且還原後會告訴你當年那組設定的字距失真多少）。

`snapshot()` 的 `rings[].characters` 鍵名維持不變，下游 `buildChakra()` 一行都不用改。

## 6. 解出的欄位鎖起來，內圈直徑跟著

自由變數解出後，該欄位**寫回畫面 + 標「由配平解出」+ 設 `readonly`**。

三個選項裡這是唯一誠實的：不寫回，欄位就顯示一個不在生效的值；寫回但可編輯，
使用者改了會被下一次重算蓋掉——**又一個「改了卻沒反應」**。鎖起來則一眼看得出它由誰決定。

同一條規則延伸到**內圈直徑 (mm)**：它平時是 N₁ 的替代輸入（打實體尺寸進去，N₁ 取整跟著變），
但 `mode === 'n1'` 時 N₁ 由配平決定，直徑就只是讀數——所以一併 `readonly`。
這個洞是實跑時才發現的：當時它可以打字，打完靜靜被還原。

原型另外兩個「欄位會自己動」的行為刻意不保留：改字級不再連帶覆寫圈距（使用者手調的值會消失），
內圈直徑不再於打字途中被 debounce 的重算搶走（只在失焦時同步回 N₁ 的實際推算值）。

## 7. 零後端

過 DATABASE_GUIDELINES §0 決策階梯：純計算、無持久化、無跨 app 共享、無寫入端 → 第 0 層。
`faber-castell-color` 是現成先例（`app.js` 只有 static + 302 + JSON 404，無 `routes/`）。

好處不只是簡單：**沒有端點就沒有路徑穿越、沒有上傳白名單、沒有 SSRF**，
§8 安全基線的每一條都因為「不存在」而滿足。

## 8. 驗證時兩個**不是 bug** 的假象

記在這裡，因為它們一定會再遇到，而且都長得像 bug。preview pane 在背景時
`document.visibilityState === 'hidden'`、`document.hasFocus() === false`，於是：

1. **程式化 `.blur()` 不會產生 blur 事件**（實測觸發次數 0），所以「內圈直徑失焦沒回寫」
   量到的是假象——改用 `dispatchEvent(new FocusEvent('blur'))` 時 handler 正常執行。
2. **Materialize modal 的開啟動畫凍結在 `opacity: 0.97319`**，畫面看起來半透明、
   底下內容透出來。強制設 `opacity: 1` 即為真實穩態。

同一輪還差點誤判第三件：截圖上 `outerExtent = …` 標籤看似被裁掉。實測
`getComputedTextLength() === getBBox().width`、末字落在 607.4／viewBox 631.43、
畫面矩形 645–790 完全落在 SVG 的 61–819 內——**沒有裁切**，是背景分頁的合成假象。

**判準：截圖是關於合成結果的陳述，不是關於 DOM 的陳述。** 兩者矛盾時以測量為準，
並先確認分頁是不是前景。（同家族 CLAUDE.md v1.14／v1.15 記載的同型教訓。）

## 9. 契約檢查（`scripts/verify.js`）

17 條，涵蓋家族規範與本 app 的不變量。值得說明的幾條：

- **① markup ↔ handler**：每個 `#setting-*` 都要在控制器裡有 `$('...')`。防的是
  PLAYBOOK §5 那三種「自動檢查看不到」的死鍵。
- **④ 動態組出的 key 用列舉、不用掃描**：`t('warn.' + code)` 這種掃不出來。
  家族 CLAUDE.md v1.9 已記載「死 key 偵測不可靠」，所以這裡反過來——**把該有的列出來逐一查**。
- **⑨ NUL 位元組**：用 `String.fromCharCode(0)` 寫，不寫實體位元組。
  家族一天內犯過三次同樣的失誤，後果是 `grep` 靜默跳過整個檔案。
- **⑬⑭⑮⑯**：幾何與契約的不變量，不是樣式檢查。

`--selftest` 會故意改壞每一條並確認都抓得到——**沒跑過反向驗證的檢查不算數**，
一支永遠回 PASS 的檢查與沒有檢查等價。

## 10. 已知限制

- **字寬假設等於字級**。CJK 成立；拉丁字母是近似值，需要精確結果應以實際字面寬換算後再輸入。
- **不排字**。本 app 只回答「每圈幾個字、多大」，不產生排好的文字路徑。
- **切換模式會沿用上一次解出的值**（解出的值會寫回欄位，成為下一個模式的輸入）。
  這是刻意的——欄位永遠顯示正在生效的值；代價是使用者原本手打的那個數字不會被保留。

## 11. 重新評估的觸發條件

以下任一成立時，回頭重看第 7 節的「零後端」與第 5 節的「深連結取代 API」：

1. 需要**儲存具名的版面預設**（深連結與 JSON 已不敷使用）。
2. 出現**第二個消費端**，且兩邊都要讀同一份設定（此時設定就成了跨 app 共享資料，
   要重跑 DATABASE_GUIDELINES §0 決策階梯）。
3. 需要**批次出圖**（多組參數一次產 SVG）——那是後端工作，不該塞進前端。
